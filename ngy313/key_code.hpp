#pragma once
#include "detail/input.hpp"

namespace ngy313 {
enum key_code {
  kDown = VK_DOWN,
  kUp = VK_UP,
  kLeft = VK_LEFT,
  kRight = VK_RIGHT,
  kSpace = VK_SPACE,
  kEscape = VK_ESCAPE,
  kZ = 'Z',
  kX = 'X'
};
}